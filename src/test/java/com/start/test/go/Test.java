package com.start.test.go;

import java.util.Arrays;
import java.util.stream.Collectors;

/**
 * @description: ${description}
 * @author: zhanghuiyong
 * @create: 2019-11-25 20:43
 */
public class Test {
    public static void main(String[] args) {
       //String address = "顾村镇星星村（远房）38号2层206室";
       //String[] result = address.split("\\d+");
        //System.out.println(Arrays.toString(Arrays.stream(result).map(s -> s.trim()).collect(Collectors.toList()).toArray()));

        int  i = 14;
        System.out.println(i);
    }
}
